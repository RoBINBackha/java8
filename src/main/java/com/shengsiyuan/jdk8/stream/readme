流由3部分构成：
1. 源
2. 零个或多个中间操作
3. 终止操作

流的操作的分类：
1. 惰性求值
2. 及早求值

stream.xxx().yyy().zzz()                  无终止操作，这行代码的中间操作是不会被执行的
stream.xxx().yyy().zzz().count()          其中的x\y\z称为惰性求值/中间操作。只有调用count方法的时候，这些中间操作才会被执行。count就是一个及早求值

流不像iterator，流是并行化的

二、流
1、Collection提供了stream()方法
2、流不存储值，通过管道的方式获取值
3、本质是函数式的，对流的操作会生成一个结果，但底层数据源不会被修改。比如集合可以作为流的底层数据源
4、延迟查找，很多流操作（过滤、映射、排序等）都可以延迟实现